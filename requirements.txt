Flask==2.3.3
Werkzeug==2.3.7
PyPDF2==3.0.1
reportlab==4.0.4
Pillow==10.0.1
python-docx==0.8.11
pdf2image==1.16.3
PyMuPDF==1.23.5
docx2pdf==0.1.8

# Optional but recommended for better performance
gunicorn==21.2.0

# For image processing (required by pdf2image)
# Note: You may need to install poppler-utils system package
# Ubuntu/Debian: sudo apt-get install poppler-utils
# macOS: brew install poppler
# Windows: Download poppler binaries and add to PATH