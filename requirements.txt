# Core Flask and web framework
Flask==2.3.3
Werkzeug==2.3.7

# PDF processing libraries
PyPDF2==3.0.1
reportlab==4.0.4
pdf2image==1.16.3
PyMuPDF==1.23.5

# Image processing
Pillow==10.0.0

# Document processing
python-docx==0.8.11
docx2pdf==0.1.8

# File handling and utilities
python-magic==0.4.27
mimetypes-extra==1.0.0

# Compression and archiving
zipfile36==0.1.3

# Font handling for Kannada support
fonttools==4.42.1

# Additional utilities
uuid==1.30
secure-filename==0.1

# Development and debugging (optional)
python-dotenv==1.0.0

# System dependencies (install via system package manager):
# Ubuntu/Debian:
# sudo apt-get install -y poppler-utils
# sudo apt-get install -y libmagic1
# sudo apt-get install -y fonts-noto-kannada
# sudo apt-get install -y libreoffice

# CentOS/RHEL:
# sudo yum install -y poppler-utils
# sudo yum install -y file-libs
# sudo yum install -y google-noto-sans-kannada-fonts
# sudo yum install -y libreoffice

# macOS (using Homebrew):
# brew install poppler
# brew install libmagic
# brew install --cask libreoffice

# Windows:
# Download and install poppler from: https://github.com/oschwartz10612/poppler-windows/
# Add poppler/bin to PATH
# Install LibreOffice from: https://www.libreoffice.org/

# Note: Some operations may require additional system-level dependencies
# For optimal Kannada font support, install Noto Sans Kannada system-wide